export const initialState = {
    item: '',
    completed: false,
    id: Math.random()
}

export function todoReducer(state, action) {
    switch(action.type) {
        default: 
            return state
    }
}